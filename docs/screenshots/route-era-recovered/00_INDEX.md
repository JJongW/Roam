# 동선(route) 추천 기능 화면 복원 — 2026-08-28

**출처**: 2026-07-08 동선 기능 제거(`8ab85d8`) 직전 마지막 커밋(`d8d60d7`)을 별도
워크트리(`.worktrees/route-era`)로 체크아웃해 mock 모드로 실행, Playwright로 실제
플로우를 밟으며 캡처했다. **당시 실제로 저장돼 있던 스크린샷은 없었다** — 이 저장소
전체 git 히스토리에 동선 시절 이미지가 하나도 없어서, 코드로 화면을 다시 띄워 재현한
기록이다.

뷰포트: 390×844(모바일).

## 화면 순서 (온보딩 → 동선 생성 → 완료)

| # | 파일 | 무엇 |
|---|---|---|
| 01 | `01_온보딩_분기질문` | "이미 가보고 싶은 부스가 있어?" — 정해둔 곳 있음 / 추천받고 싶음 분기 |
| 02 | `02_부스검색_다중선택` | "가고 싶은 부스를 골라줘" — 부스명·출판사·코드 검색 + 다중 체크 |
| 03 | `03_관심태그_칩선택` | 직접체험·굿즈·새기술 등 관심사 태그 칩 다중 선택 |
| 04 | `04_시간예산_질문` | "얼마나 여유롭게 둘러볼 수 있어?" — 30분/1시간/2~3시간/상관없음 |
| 05 | `05_스타일선택` | "AI에게 맡길게" 등 동선 스타일(천천히·인기부스 위주·숨은부스 등) |
| 06 | `06_동선결과_번호부스목록` | ⭐ 핵심 — 번호 매겨진 추천 부스 1~6, "맘에 안 드는 건 빼도 돼"(X로 제거) |
| 07 | `07_전시홈_동선완료후` | 동선 확정 후 전시홈 — "다른 사람 동선 둘러보기" 메뉴 노출 |
| 08 | `08_지도_범례` | 지도 색 범례: 방문(초록)·관심(노랑)·**동선(보라)** |
| 09 | `09_지도_사용법코치마크` | 지도 첫 진입 코치마크(AI추천·메모장·커뮤니티·부스색 설명) |
| 10 | `10_내동선_빈상태` | "담은 부스가 없어요" — 동선 담기 전 빈 상태 |

## 재현 한계

- 이번 재현에서는 동선 확정 후에도 `GET /api/me/routes`가 빈 배열을 반환했다 — 온보딩
  마법사의 "이 N곳으로 시작하기"가 실제 저장(POST)까지 이어지는 스텝을 하나 더
  거쳤어야 했을 가능성이 있다(당시 실제 서비스에서는 저장됐을 것). 그래서 지도 위에
  실제 보라색 동선 라인이 그려진 화면은 못 건졌다.
- 캡처는 mock 데이터(2026 서울국제도서전 시드) 기준이라 실제 배포본 데이터와는 다르다.
